# AGENTS.md

> Файл для AI-агентов, работающих с проектом. Проект написан на русском языке, все комментарии и документация внутри кода — на русском.

---

## Обзор проекта

Это исследовательский проект (дипломная работа) по предобработке осциллограмм электросети в формате COMTRADE (IEEE Std C37.111) для задачи локализации короткого замыкания (КЗ) с помощью машинного обучения.

Основной файл проекта — Jupyter Notebook `fault_location_preprocessing_pipeline.ipynb`, который содержит полный end-to-end пайплайн обработки одного COMTRADE-файла от сырых данных до готового тензора для нейросети.

### Что делает пайплайн

1. **Парсинг COMTRADE** — читает `.cfg` (метаданные) и `.dat` (осциллограмма).
2. **Фильтрация Баттерворта** — ФВЧ 4-го порядка, fc=25 Гц, удаляет апериодическую (DC) составляющую.
3. **Детекция момента КЗ** — по производной тока с порогом `eta * Inom`.
4. **Оконирование** — вырезает окно `[-50 мс, +150 мс]` относительно момента КЗ.
5. **Симметричные составляющие (Фортескю)** — вычисляет `I₀, I₁, I₂` и `U₀, U₁, U₂`.
6. **Классификация вида КЗ** — по отношениям `I₂/I₁` и `I₀/I₁` определяет тип: `3ф`, `2ф`, `2ф-З`, `1ф-З`.
7. **Нормализация в p.u.** — приводит токи и напряжения к относительным единицам.
8. **Формирование тензора** — собирает `X ∈ R^(10 × L_window)` и целевую переменную `y` (расстояние до КЗ в p.u.).

---

## Структура директорий

```
Jupiter_Notebook/
├── fault_location_preprocessing_pipeline.ipynb   # Основной ноутбук
├── data/
│   ├── 3.1.1(K3, 1A).cfg                         # Исходный COMTRADE (конфиг)
│   └── 3.1.1(K3, 1A).dat                         # Исходный COMTRADE (данные)
├── filtered_data.cfg                             # Результат фильтрации (конфиг)
├── filtered_data.dat                             # Результат фильтрации (данные)
└── AGENTS.md                                     # Этот файл
```

**Важно:** проект содержит всего один активный файл кода — Jupyter Notebook. Все функции (парсинг, фильтрация, визуализация, классификация) реализованы прямо в ячейках ноутбука.

---

## Технологический стек

- **Язык:** Python 3.10+
- **Среда:** Jupyter Notebook (ipykernel)
- **Основные библиотеки:**
  - `numpy` — вычисления, работа с массивами
  - `pandas` — табличные данные
  - `matplotlib` — визуализация
  - `scipy` (`scipy.signal`) — фильтрация, спектральный анализ
  - `comtrade`, `comtradehandlers` — чтение COMTRADE-файлов

### Установка зависимостей

В первой ячейке ноутбука выполняется:

```python
!pip install numpy pandas matplotlib scipy comtrade comtradehandlers
```

Нет отдельного `requirements.txt` или `pyproject.toml` — зависимости устанавливаются inline.

---

## Организация кода

Код организован по ячейкам в логическом порядке:

| Раздел | Содержимое |
|--------|-----------|
| Импорты | `numpy`, `pandas`, `matplotlib`, `scipy`, `struct`, `re`, `dataclasses` |
| Раздел 1 | `find_comtrade_files()`, `inspect_channels()`, `parse_comtrade()`, `plot_phases()` |
| Раздел 2 | `butterworth_highpass()`, `get_dc()`, сохранение отфильтрованного COMTRADE |
| Раздел 3 | `detect_fault_inception()` |
| Раздел 4 | Оконирование сигнала |
| Раздел 5 | `fortescue_instantaneous()` — мгновенные симметричные составляющие |
| Раздел 6 | `classify_fault_type()` — one-hot кодировка типа КЗ |
| Раздел 7 | Нормализация в p.u. по параметрам линии (`LINE_PARAMS`) |
| Раздел 8 | Сборка тензора `X`, цели `y`, сохранение в `.npz` |

### Ключевые параметры

- `FS = 5000` — частота дискретизации (Гц)
- `FC = 25.0`, `ORDER = 4` — параметры фильтра Баттерворта
- `T_PRE = 0.050`, `T_POST = 0.150` — окно анализа (сек)
- `LINE_PARAMS` — параметры линии для расчёта базовых величин (`Unom_kv`, `L_km`, `r1_ohm_km`, `x1_ohm_km`)

---

## Данные

### Исходные данные

- **Файл:** `data/3.1.1(K3, 1A).cfg` / `.dat`
- **Источник:** RTDS Technologies (симуляция)
- **Формат:** COMTRADE 1999, ASCII
- **Каналы:** 59 аналоговых + 28 дискретных
- **Частота дискретизации:** 5000 Гц
- **Количество отсчётов:** 50000

### Выходные данные

- `filtered_data.cfg` / `filtered_data.dat` — отфильтрованные токи и напряжения (6 каналов: IA, IB, IC, UA, UB, UC)
- `fault_sample_processed.npz` — NumPy-архив с тензором `X`, меткой `y` и метаданными

---

## Соглашения по стилю

- **Язык кода и комментариев:** русский.
- Имена переменных: `snake_case` для функций и переменных, `SCREAMING_SNAKE_CASE` для констант.
- Функции снабжены docstrings на русском языке.
- После каждого этапа обработки строятся графики с разметкой для визуальной проверки корректности.
- Фазы тока/напряжения обозначаются как `IA, IB, IC` и `UA, UB, UC`.
- Симметричные составляющие: `I0, I1, I2` (нулевая, прямая, обратная).

---

## Тестирование

Формального тестового фреймворка (pytest, unittest) в проекте нет. Валидация происходит через:

1. **Визуализацию** — после каждого шага строятся графики исходных/обработанных сигналов.
2. **Печать промежуточных значений** — выводятся ключевые метрики (амплитуды, отношения, ошибки детекции).
3. **Ручную проверку каналов** — функция `inspect_channels()` позволяет убедиться, что выбраны правильные номера каналов из CFG.

---

## Инструкции для агента

- **Не создавай лишних файлов.** Проект intentionally minimalistic — вся логика в одном ноутбуке.
- Если нужно добавить новый функционал (например, обработку пакета файлов), добавляй его в виде новых ячеек в конец ноутбука или в отдельный `.py`-файл только при явной необходимости.
- **Сохраняй русский язык** для всех комментариев, docstrings и markdown-ячеек в ноутбуке.
- При изменении параметров фильтрации или оконирования — обязательно обновляй и соответствующие графики/выводы.
- Если добавляешь новые зависимости — добавь ячейку с `!pip install ...` в начало ноутбука.

---

## Безопасность и ограничения

- Проект работает с локальными файлами внутри рабочей директории. Не пиши за пределы `Jupiter_Notebook/`.
- COMTRADE-файлы могут быть большими; при обработке множества файлов следи за потреблением RAM.
- Нет секретов, API-ключей или учётных данных в проекте.
